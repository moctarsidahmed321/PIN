{% extends 'base.html.twig' %}

{% block title %}Page d'acceuil{% endblock %}

{% block body %}
    <div class="mt-5">
        <h3>Nouvelles</h3>
        <hr>
        <ul class="mx-6">
            {% for nouvelle in nouvelles %}
                <li>{{ nouvelle.nouvelles }}</li>
            {% endfor %}
        </ul>

    </div>

    <div>
        <h3>Présentation</h3>
        <hr>
        <div class="mx-6">
            {% for presentation in presentations %}
                {{ presentation.nouvelles|raw }}
            {% endfor %}
        </div>

    </div>


{% endblock %}
