{% extends 'base.html.twig' %}

{% block title %}Intervenant{% endblock %}

{% block body %}
    <h1>Intervenant</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ intervenant.id }}</td>
            </tr>
            <tr>
                <th>Date de Passage</th>
                <td>{{ intervenant.datePassage ? intervenant.datePassage|date('Y-m-d H:i') : '' }}</td>
            </tr>
            <tr>
                <th>Heure de fin</th>
                <td>{{ intervenant.heureFin ? intervenant.heureFin|date('H:i') : '' }}</td>
            </tr>
            <tr>
                <th>Sujet</th>
                <td>{{ intervenant.sujet }}</td>
            </tr>
            <tr>
                <th>Biographie</th>
                <td>{{ intervenant.biographie }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ intervenant.contenu }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('intervenant_edit', {'id': intervenant.id}) }}" class="btn btn-outline-success">Modifier</a>
    <br>
    <a href="{{ path('intervenant_index') }}" class="btn btn-outline-secondary mt-1">Retour à la liste</a>

    {{ include('intervenant/_delete_form.html.twig') }}
{% endblock %}
