{{ form_start(form) }}
    {{ form_errors(form.nouvelles) }}
    {{ form_row(form.nouvelles) }}
    {{ form_row(form.type) }}
   <button class="btn btn-outline-success">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
